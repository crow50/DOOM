Application test suite — ASVS 5.0 L1/L2 closure candidate
=========================================================
Run 2026-09-24 on the review host, against a real PostgreSQL 16 instance
(the suite refuses to run on SQLite: the CITEXT case-insensitive unique index
and the num_nonnulls CHECK constraints it verifies exist only in Postgres).

This is a HOST run, not the containerised one. It establishes that the source
in this branch passes its own suite; it does not replace
`python3 tools/run_security_review.py`, which builds the image, runs the suite
inside it, and exercises the restricted database role, the secret checks and
the proxy probes. That run is stale against this source and is recorded as
such in verification.json.

.....................                                                    [100%]
=============================== warnings summary ===============================
tests/test_security_review.py::test_image_pixel_limit_is_a_hard_limit
  /tmp/claude-0/-home-user-DOOM/ade63df2-0efc-5e38-909e-dbea6bea15df/scratchpad/venv/lib/python3.11/site-packages/PIL/Image.py:3578: DecompressionBombWarning: Image size (110 pixels) exceeds limit of 100 pixels, could be decompression bomb DOS attack.
    warnings.warn(

-- Docs: https://docs.pytest.org/en/stable/how-to/capture-warnings.html
453 passed, 1 warning in 53.93s

Migration check (alembic head applied and matching the models):
  flask db upgrade   -> 372bba4db7ea, then 8f21c4a9de07
  flask db downgrade base && flask db upgrade -> reapplied cleanly
  flask db check     -> No new upgrade operations detected.
